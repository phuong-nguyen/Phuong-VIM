[user]
  name = Phuong Nguyen
  email = phuongnd08@gmail.com
[alias]
  ci = commit
  co = checkout
  st = status --short
  lg = log --oneline --decorate -n 20
  wd = diff --word-diff
  dc = diff --cached
  wdc = diff --cached --word-diff
  pom = push origin master
  po = push origin
  ca = commit --amend
  ru = remote update
  ruo = remote update origin
[github]
  user = phuongnd08
[color]
  diff = auto
  status = auto
[color "status"]
  changed = yellow
  added = green
  untracked = red
[diff]
  tool = meld
[difftool "meld"]
  cmd = meld\"$LOCAL\" \"$REMOTE\"
[mergetool "meld"]
  trustexitcode = false
  cmd = meld --merge --result=$MERGED $LOCAL $BASE $REMOTE
[merge]
  tool = meld
[mergetool]
  keepBackup = false
[core]
  editor = /usr/bin/vim
